fastapi==0.115.6
uvicorn[standard]==0.34.0
pandas==2.2.3
sqlalchemy[asyncio]>=2.0.40
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.1
redis==5.2.1
python-dotenv==1.0.1
websockets==14.1
claude-agent-sdk==0.1.50
mcp>=1.0,<2
openai>=1.40,<2
httpx==0.28.1
feedparser==6.0.11
apscheduler==3.10.4
loguru==0.7.3
pydantic-settings==2.7.1
lightgbm==4.6.0
scikit-learn==1.5.2
statsmodels>=0.14.0
joblib==1.4.2
PyJWT[crypto]>=2.10.1
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
webauthn==2.7.1
psutil==6.1.1
arch>=7.0
hmmlearn>=0.3
filterpy>=1.4
cvxpy>=1.4
sentry-sdk[fastapi]==2.20.0

# Laya 决策引擎（默认关闭 laya_enabled=False，未装也能启动——try-import 降级）
# NOTE: laya 依赖 torch>=2.0/transformers。Dockerfile 已先装 CPU-only torch
# （--index-url https://download.pytorch.org/whl/cpu），避免拉取 CUDA 版 (~2GB+)。
# 权重从 HF 下载（~1.7GB english / 1.3GB multilingual），部署用 HF_ENDPOINT 镜像
# 或构建期预缓存（见 Dockerfile laya-model 阶段注释）。
# C6 修复：显式 pin huggingface-hub（≥1.0 移除了 huggingface-cli 子命令，用 snapshot_download）
# 与 transformers，避免 laya 的宽依赖锥（hub>=0.20 / transformers>=4.45 无上界）解析到不可复现版本。
laya==0.3.4
huggingface-hub>=0.26,<2
transformers>=4.45,<5
